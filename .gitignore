node_modules
.next
out
*.db
*.db-journal
.env.local
next-env.d.ts
tsconfig.tsbuildinfo
__pycache__

# Tajne: HMAC kljuc, token secret, admin token. Vidi .env.example.
.env

# Privatni store: originali i peceni derivati. Nikad javna putanja.
storage

# Dodao Vercel CLI pri `vercel link`. Njegov `.env*` je suzen na .env.local —
# `.env` je vec gore, a .env.example se namjerno komituje.
.vercel
.env.local
